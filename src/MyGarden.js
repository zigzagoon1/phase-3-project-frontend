import React from "react";

//originally designed as though for a single user, can add user system later
//shows each garden area with all its plants that the user has created

function MyGarden() {
    return (
        <div>

        </div>
    )
}

export default MyGarden;